# This project demonstrate the use of Sumsub API 

#     db_url = 'expendier'
#     # db_user = os.environ.get('postgresql_ex_user')
#     # db_password = os.environ.get('postgresql_ex_password')
#     # db_host = os.environ.get('postgresql_ex_host')
#     # db_port = os.environ.get('postgresql_ex_port')
#     # db_url = f'postgresql://{db_user}:{db_password}@{db_host}:{db_port}/{db_name}'

# openssl rand -hex 32

# // Example in JavaScript (Frontend)
# function logout() {
#     localStorage.removeItem("access_token");  // or sessionStorage
#     // Redirect user to login page or home page
#     window.location.href = "/login";
# }
